# Get a free API key from https://aistudio.google.com/apikey
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.6-flash

# Google Search grounding (live up-to-date answers).
# NOTE: NOT available on the free tier (requires billing on Google Cloud).
# Set to true only after enabling billing on the project that owns this key.
ENABLE_GROUNDING=false